__pycache__/
*.pyc
.venv/
.vscode/
.idea/

# PyInstaller output
build/
dist/
*.spec.bak
log_*.html

# Nexus page draft — lives in the repo locally as reference, not for distribution
NEXUS_PAGE.md

# Local context-handoff doc — used for resuming work in a new Claude chat
CONTEXT.md

# Generated artifacts — regenerated by build_exe.bat (embed_nut.py + build_zip.py).
mod_template.py
mod/mod_bb_reroll_dump.zip
mod/build/

# Superseded by the v3.3.0-stable git tag; listed in case it gets recreated.
mod/_backup_v3.3.0-stable/

# Local deploy target (machine-specific BB data path). Copy from the example.
deploy_config.json
