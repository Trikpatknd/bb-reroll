"""BB Reroll helper package.

gui.py (the entry point) stays at the repo root; the supporting modules live
here: log-line recognition (logscan), the per-origin hardcoded-stars catalog
(origin_hardcoded_stars), the trait display-name map (trait_names), and the
generated embedded mod source (mod_template — produced by tools/embed_nut.py,
gitignored).
"""
